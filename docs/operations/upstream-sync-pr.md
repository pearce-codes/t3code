## Upstream baseline

This pull request merges the current `pingdotgg/t3code` `main` branch into Pearce Codes.

## Required verification

- [ ] Review conflicts by downstream feature, not only by file.
- [ ] Run focused tests for every affected package.
- [ ] Verify Pearce identity, data-home isolation, CLI naming, and release metadata.
- [ ] Verify Kiro turns, plans, tools, subagents, and usage rendering.
- [ ] Verify web, desktop, and mobile behavior where the diff applies.
- [ ] Run `vp run icons:check` when brand assets or packaging changed.
- [ ] Run `node scripts/release-smoke.ts` before promoting a release.

This pull request must be reviewed and merged manually.
